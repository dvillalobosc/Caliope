To Do list:

[General]
* Review gramatical errors in the entire project.
* Enable session storing.
** Show query result as is is processed.
** View source in DWebView. It has an error in Qt5. See DWebView::showSourceCodeTriggered()
** Enable the Scheme Color in Preferences, so we can change the color of the wigets.
** Check why sqlHighlightRules() is called twice.
** Fix bug in MainWindow::openURLSlot(QString url), I think it is a Qt thing.
** Make F7 Works with formal names.
** Make TextEditor inherit from QWidget.
** Document Keyword shortcuts BaseTextEditor::keyPressEvent(QKeyEvent *event)
** Implement the Replicaction improvements for MariaDB 10
